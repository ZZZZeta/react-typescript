const name = 'posts';

export const GET_POSTS_REQUEST = `${name}/GET_POSTS_REQUEST`;
export const GET_POSTS_SUCCESS = `${name}/GET_POSTS_SUCCESS`;
export const GET_POSTS_FAILURE = `${name}/GET_POSTS_FAILURE`;

export default name;
